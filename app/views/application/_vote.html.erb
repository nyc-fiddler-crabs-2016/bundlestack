<div class="votes-partial">
  <p><%= link_to "UP", up_vote_path(object) %></p>
  <% if object.votes.any? %>
    <p><%= object.total_votes%></p>
  <% end %>
  <p><%= link_to "DOWN", down_vote_path(object) %></p>
</div>
